# 🥞 Stack — Linear Data Structure

## 🎯 Concept
Stack follows the **LIFO (Last In, First Out)** principle.  
The last element pushed is the first one to be popped.

---

## ⚙️ Operations

| Operation | Description | Time Complexity |
|------------|--------------|-----------------|
| `push()` | Adds element to top | O(1) |
| `pop()` | Removes top element | O(1) |
| `peek()` | Returns top element without removing | O(1) |
| `isEmpty()` | Checks if stack is empty | O(1) |

---

## 🧠 Real-Life Analogies
- Stack of plates → Only top plate accessible.  
- Browser back button → Pop last visited page.  
- Undo operation → Remove last action.

---

## 🚫 Overflow / Underflow
- **Overflow:** Inserting into a full stack.  
- **Underflow:** Removing from an empty stack.

---

## 🧩 Applications
- Expression evaluation (Infix → Postfix)  
- Balanced parentheses check  
- Function call management (recursion stack)  
- Undo/Redo system  

---

> ✨ “Stack remembers history — but only the most recent mistake first.” ✨
